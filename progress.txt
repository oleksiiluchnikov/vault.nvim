2026-03-12
- Built initial Neovim-native E2E harness under `tests/e2e/`.
- Harness launches a fresh headless Neovim process with its own `NVIM_APPNAME` and `--listen` socket.
- Harness clones the source vault into a disposable temp directory and refuses to target the live `~/knowledge` root directly.
- `tests/minimal_init.lua` now accepts `VAULT_TEST_ROOT`, `VAULT_TEST_LINES`, `VAULT_TEST_COLUMNS`, and `VAULT_TEST_APPNAME` overrides for isolated E2E runs.
- `tests/e2e/foundation_spec.lua` proves that a real `:Vault note new ...` command mutates only the cloned vault and emits a diff artifact.
- Next step: implement the first real user journey E2E on top of the harness, starting with taxonomy/process (`:Vault classify`).
2026-03-12
- Added `tests/e2e/taxonomy_process_spec.lua` with three real journeys: process edit/save/undo, classify edit/save, and taxonomy preview/apply/undo-last.
- Driver control needed to be synchronous for deterministic E2E: `driver.command()` and `driver.lua()` now execute through `--remote-expr execute(...)` instead of async `--remote-send`.
- For first-grid journeys, Ex substitutions inside the child Neovim were more reliable than trying to mutate process buffers via ad hoc Lua snippets.
- The first strong E2E seam is confirmed: command -> grid/taxonomy UI -> file mutation -> undo.
- Next step: tasks lifecycle E2E journeys (`tasks promote`, `status`, `pick-next`, `recur`, `doctor`).
2026-03-12
- Added `tests/e2e/tasks_lifecycle_spec.lua` covering `tasks promote`, `tasks status`, `tasks pick-next`, `tasks recur now`, `tasks recur sweep`, and `tasks doctor fix` on disposable cloned vaults.
- Early task E2E exposed path-escaping quirks; the stable fix for assertions was to compare on basename or use space-safe fixture filenames where current-task detection matters.
- The command-driven task lifecycle is now covered through real `:Vault tasks ...` entrypoints, not direct helper calls.
- Next step: resolve-picker note/tag workflows (`merge`, `retarget`, `promote`) using the same harness.
2026-03-12
- Added `tests/e2e/resolve_picker_workflows_spec.lua` covering note merge, note retarget create, and tag promote through the real resolve-picker UI.
- Merge flow revealed a second interactive stage: after target selection, the merge conflict UI needs a final `<CR>` apply step.
- Tag promote E2E is more reliable when it tests create-target flow through `<C-n>` instead of depending on fuzzy selection of an existing target.
- Resolve-picker journeys now cover both rewrite and create outcomes through real interactive input.
- Next step: duplicate review E2E journey, then kanban/calendar plus picker health smoke.
